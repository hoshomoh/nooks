/**
 * The facts more than one TypeScript app has to state.
 *
 * Where the project lives, where an assistant connects, what a client's configuration
 * looks like. Said twice they drift, and the drift is invisible until somebody follows
 * the wrong copy.
 */
export * from "./mcp"
export * from "./site"
