# Read-only prior-art clones and local working notes. Never edited, never shipped.
reference/
.local/

# Dependencies
node_modules/

# Build output
apps/web/dist/
/nooks

# The SPA that `pnpm --filter @nooks/web release` bakes into the binary. Only the
# marker is tracked, so //go:embed still compiles on a fresh clone and a binary built
# without the app says so instead of serving a page that cannot load.
server/router/frontend/dist/*
!server/router/frontend/dist/.gitkeep

# Local data — a Nooks instance keeps its SQLite file here during development
*.db
*.db-shm
*.db-wal
/data/

# Machine-specific editor and tooling settings
.claude/
.agents/

# Downloaded toolchain archives
go*.linux-amd64.tar.gz

# Editors and OS
.DS_Store
.idea/
.vscode/

# The checkout ./scripts/preflight.sh verifies a commit in.
.preflight/
